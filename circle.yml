machine:
 node:
   version: 4.7.0
dependencies:
  cache_directories:
    - "~/nvm"
  pre:
    - npm -g install ember-cli@2.3.0
    - npm -g install bower
    - bower cache clean
    - npm cache clean
  override:
    - npm install
    - bower install
test:
  override:
    - npm test
